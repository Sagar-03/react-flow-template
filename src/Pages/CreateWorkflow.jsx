import React from "react";
// import ReactFlow from 'reactflow'; // Uncomment and install if using React Flow

const CreateWorkflow = () => {
  return (
    <div className="container-fluid p-4">
      <h2>Create New Workflow</h2>
      {/* Place your React Flow component here */}
      {/* <ReactFlow ... /> */}
      <div style={{height: 400, border: '1px solid #ccc', borderRadius: 8, marginTop: 20, display: 'flex', alignItems: 'center', justifyContent: 'center'}}>
        <span>React Flow goes here</span>
      </div>
    </div>
  );
};

export default CreateWorkflow;
